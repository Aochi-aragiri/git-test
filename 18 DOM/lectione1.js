/* function countVowels(str) {
  let vowelsCount = 0;
  for (let i = 0; i < str.length; i++) {
    const character = str[i];
    if (vowels.includes(character.toLowerCase())) {
      vowelsCount++;
    }
  }
  console.log(`Vowels count for ${str} = ${vowelCount}`);
}

countVowels(`Hello, World!`);
countVowels(`HELLO WORLD!`);

function reverseString(str) {
  /*  const arr = str.split('');
  const reversedArr = arr.reverse();
  const finalStr = reversedArr.join(''); */
/*   console.log(str.split("").reverse().join(""));
} */

/* function test(a, cb) {
  cb(a, 'testparam');
} */

/* function callback(a) {
  console.log(a);
} */

// test(1, callback);
/* const timeoutId = setTimeout(() =>{
  console.log('timeout');
}, 1000);

clearTimeout(timeoutId);

setinterval(() => {
  console.log('interval'); // декілька раз
}, 1000);

setTimeout(() => {
  clearInterval(invalidId); // один раз
}, 5000); */

// Math

/* function degToRsd(deg) {
  return deg * (Math.PI / 180);
}

console.log(Math.PI);
console.log(Math.round(3.5)); // заокруглення математичне
console.log(Math.ceil(3.5)); // заокруглення до найбільшого
console.log(Math.floor(3.5)); // до найменшого
console.log(Math.round(3.5));
console.log(Math.random());
console.log(Math.max(3.5));
 */
/* function randomRange(min, max) {
  return Math.random() * 10;
  // return Math.round(Math.random() * (max - min) + min);
}

console.log(randomRange(3, 10)); */

// console.log(Math.pow(3, 3)); // 27
